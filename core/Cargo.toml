[package]
name = "zboot-core"
description = "Pure-data core: ZFS property parsing, BE forest, atomic-set semantics for zboot."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
